# TabICLv2 Regressor standalone Colab tutorials

[![GitHub](https://img.shields.io/badge/GitHub-181717?style=flat&logo=github&logoColor=white)](https://github.com/kurtvalcorza/tabicl-regressor-pipeline)
[![Open In Colab](https://colab.research.google.com/assets/colab-badge.svg)](https://colab.research.google.com/github/kurtvalcorza/tabicl-regressor-pipeline/blob/main/tutorials/tabiclv2_regressor_colab.ipynb)
[![Hugging Face](https://img.shields.io/badge/%F0%9F%A4%97%20Hugging%20Face-jingang%2FTabICL-ffcc4d?style=flat)](https://huggingface.co/jingang/TabICL)
[![Upstream](https://img.shields.io/badge/Upstream-soda--inria%2Ftabicl-181717?style=flat&logo=github&logoColor=white)](https://github.com/soda-inria/tabicl)
[![arXiv](https://img.shields.io/badge/arXiv-2602.11139-b31b1b.svg)](https://arxiv.org/abs/2602.11139)

Notebook specification: **DIMER Notebook Specification 2.0** — both notebooks are **standalone** (§4): they are generated by `tools/build_notebook.py` from `tools/notebook_template.py` (E2E) and `tools/notebook_template_artifact_inference.py` (companion) and carry the repository's public reference API module (`src/tabicl_regressor_pipeline/api.py`), model identity, snapshot manifest and runtime pins, so the exported `.ipynb` files keep working without this repository being reachable. Do not edit a notebook by hand; edit the package or the template and regenerate (`python tools/build_notebook.py` and `python tools/build_notebook.py --template tools/notebook_template_artifact_inference.py`; `--check` is enforced by the validator and CI). A notebook is promoted from Candidate to Release-grade only after clean-runtime execution evidence is recorded for the exact release revision in [`../docs/release-verification.md`](../docs/release-verification.md).

| Notebook | Profile | Mode | Carrier | Capability | Default runtime | Sample | BYOD / external input | Run-all | Release status |
|---|---|---|---|---|---|---|---|---|---|
| [`tabiclv2_regressor_colab.ipynb`](tabiclv2_regressor_colab.ipynb) | `E2E` | `GUIDED` | standalone (generated) | `validate_inputs` → input manifest; pretrained in-context evaluation on holdout + independent test with MAE, MSE, RMSE, R², Pearson r against `training_mean_baseline`, LightGBM and Random Forest (same partitions) plus in-memory blending; optional CUDA fine-tuning with holdout-only selection; `evaluation_report` → `sample-sanity`; optional new-data point prediction; DIMER-style bundle export (checkpoint + training context + manifest) and fresh reload with prediction equivalence | CPU (CUDA used automatically when available; fine-tuning needs CUDA) | automatic | `DATA_SOURCE` upload options gated by `USE_BYOD`; `RUN_FINE_TUNING`, `RUN_NEW_DATA_INFERENCE` off by default | verified — clean-runtime `Run all` execution recorded (REL1/REL8) | **Candidate** — static checks pass; the clean-runtime execution run of the standalone carrier is clean-room execution recorded in `../docs/release-verification.md`, which must be reviewed for the exact notebook revision before promotion |
| [`tabiclv2_regressor_artifact_inference_colab.ipynb`](tabiclv2_regressor_artifact_inference_colab.ipynb) | `ARTIFACT-INFERENCE` | `GUIDED` | standalone (generated) | External bundle ZIP: member-by-member safe extraction, allowlist/size/digest verification, runtime compatibility and base-model identity checks before reconstruction; in-context regressor rebuilt from the bundle alone; `validate_inputs(target_column=None)` → input manifest for new unlabelled rows; point predictions; `evaluation_report` → `not-measurable`; export | CPU (CUDA used automatically when available) | **none yet** — default path uploads both the bundle and the rows (§19 SART1/RUN5 gap) | bundle ZIP + one unlabelled CSV, by upload or `ARTIFACT_ZIP_PATH` / `NEW_DATA_PATH` (no bundled sample: the bundle must come from a separate execution) | verified — clean-runtime `Run all` execution recorded (REL1/REL8) | **Candidate** — external-artifact clean-runtime evidence pending; see `../docs/release-verification.md` |

## Conformance notes

_Requirement identifiers in these notes (for example `ST1`, `PAR3`, `DAT24`, `EVAL21`) are the NOTEBOOK_SPEC 1.1 numbering they were written against; NOTEBOOK_SPEC 2.0 renumbered its sections and the pull request that migrated this repository reports conformance against the 2.0 pre-flight checklist (§26) and report format (§30)._

- **Standalone carrier (ST1–ST6):** the primary path performs no clone, repository install or repository import (the previous pair's `git+https://github.com/kurtvalcorza/...` install at an immutable commit is gone: the module is carried instead). Section 2 of each notebook is `src/tabicl_regressor_pipeline/api.py` verbatim (one cell tagged `metadata.dimer.embedded_module`; the only rewrite makes `DEFAULT_WEIGHTS_DIR` working-directory-relative), Section 3 carries the model identity and the one-file SHA-256 manifest inline and asserts they agree with the module before anything is fetched, and Section 1 carries the exact `pyproject.toml` runtime pins (`torch==2.11.0` is now installed from the pins rather than assumed runtime-provided). `metadata.dimer.generated_from` records the repository, the generating commit, the module SHA-256 and the generator version.
- **Parity (PAR1–PAR4):** `tests/test_notebook_parity.py`, `tests/test_companion_parity.py` and `tools/validate_release_assets.py` fail when the carried module, the inline manifest, or the inline pins differ from the repository, or when a notebook is not byte-identical to the generator's output for its template.
- **Model acquisition:** `stage_missing_files(WEIGHTS_DIR, allow_download=True)` fetches only the manifest entry that is absent, from the Hugging Face Hub at the immutable revision (never `main`); `verify_snapshot` re-hashes it and asserts the manifest digest equals the package's `BASE_MODEL_SHA256`; `TabICLRegressionPipeline.from_pretrained(weights_dir=WEIGHTS_DIR, ...)` constructs the upstream estimator on the verified file with `allow_auto_download=False`. The previous `CHECKPOINT_SOURCE = "DIMER ZIP"` option is replaced by the snapshot directory: place the pinned `.ckpt` there and nothing is downloaded.
- **Role stages:** `validate_inputs` (DAT24) writes `outputs/<stem>_input_manifest.json` with the schema, the observed structure of every table (dropped non-finite-target rows are counted, never hidden — DAT23), the verdict and one recorded rejection finding; `evaluation_report` (EVAL21) writes `outputs/<stem>_evaluation_report.json` — `sample-sanity` with `mae` / `mse` / `rmse` / `r2` / `pearsonr`, the independent-test block and the training-mean baseline in the E2E notebook (classical baselines and the blend are attached), `not-measurable` (with what labelled data would make the task measurable) in the companion.
- **Artifact-inference companion (§18):** the bundle is supplied from outside the execution (upload, or `ARTIFACT_ZIP_PATH` for executors), extracted member by member by `safe_extract_zip` only after path/symlink/size checks (never `extractall`), verified by `verify_artifact_bundle` and `validate_artifact_runtime`, and compared with the carried pinned base-model identity before any model state is reconstructed; reconstruction uses the bundled checkpoint with `allow_auto_download=False` (no network fallback); no bundle is created and no fine-tuning happens in the companion.
- Regression obligations (Notebook Specification §20.2): the target is validated as finite numeric and varying before the model runs, MAE and RMSE are reported in target units with R² alongside, and the notebooks state that predictions are point estimates with no uncertainty interval.
- `USE_BYOD`, `RUN_FINE_TUNING` and `RUN_NEW_DATA_INFERENCE` default to `False` so the E2E sample path never opens an upload dialog and never trains.
- `tools/validate_release_assets.py` performs source validation only. It does not satisfy the clean-runtime execution requirement; a release review must confirm that a recorded clean run in `../docs/release-verification.md` matches the notebook revision under review before the status is promoted to `Release-grade`.

## Runtime and model contract

- The runtime pins are the `pyproject.toml` `[project].dependencies` (`==` only), carried inline by both notebooks as `PINS`: the release-verified [`requirements-release.txt`](requirements-release.txt) set (whose transitive resolution is recorded in [`requirements-release.lock`](requirements-release.lock)) plus `torch==2.11.0` and `numpy==2.5.3`. `huggingface-hub` stays at the lock's 1.30.0 because `transformers==5.6.2`, needed by the opt-in fine-tuning path, requires `huggingface-hub>=1.5.0`.
- Hugging Face model: `jingang/TabICL` at immutable revision `4dcd344ece2c00be9e831fdd35bed57b5ad83e19`; checkpoint `tabicl-regressor-v2-20260212.ckpt`, SHA-256 `0db9cb538f114e79026bf08f45f41ad8dd7ad2de2aaca9a5ca8cd3bd9748ae7a`; local snapshot layout `weights/tabicl-regressor-v2/` with the committed `dimer-base-manifest.json` (see [`../docs/WEIGHTS.md`](../docs/WEIGHTS.md)).
- Both notebooks print effective Python/framework/model runtime identity in Section 1 and again in the exported result JSON.

## Main tutorial

- exact pinned `tabicl-regressor-v2-20260212.ckpt` acquisition through the manifest-driven snapshot (`stage_missing_files` + `verify_snapshot`)
- Diabetes sample (default), California Housing subsample, or BYOD CSV / pre-split `train.csv`/`val.csv`/`test.csv`
- finite numeric-target validation with dropped-row counting, row/feature ceilings and a training-target variation check (`validate_inputs`, `prepare_regression_table`)
- pretrained in-context evaluation with MAE, MSE, RMSE, R², and Pearson correlation (`regression_metrics`)
- optional `FinetunedTabICLRegressor` CUDA fine-tuning behind `RUN_FINE_TUNING` with holdout-only selection (`compare_metric`); independent test is evaluation only
- companion classical tree baselines (LightGBM and Random Forest) with holdout leaderboard and device latency
- in-memory post-hoc point-prediction blending with holdout RMSE minimisation and generalisation assessment
- `evaluation_report` with the training-mean baseline, the classical baselines and the blend attached
- optional new-data point prediction behind `RUN_NEW_DATA_INFERENCE` (inference-mode `validate_inputs`)
- DIMER-style bundle export: checkpoint + training context + manifest (digests, sizes, payload allowlist, producer runtime)
- fresh reload through `safe_extract_zip` + `verify_artifact_bundle` with a prediction-equivalence check

## Artifact contract

TabICL remains an in-context learner after downstream fine-tuning. A deployable bundle therefore requires at minimum:

```text
checkpoints/best.ckpt
training_context.parquet
artifact.json
```

The inference-only notebook verifies internal digests, reconstructs the support context, and calls the ordinary TabICL estimator with `allow_auto_download=False`.

## Trust boundary

ZIP path checks prevent traversal and symlink extraction, but they do not make model deserialization trustworthy. Load only artifacts you produced yourself or obtained from a trusted source. When a known ZIP SHA-256 is available, compare it before extraction/loading.

The portable artifact also embeds the labelled training context. Treat the exported ZIP with the same dataset licence, access-control, retention, and disclosure requirements as the source training data.

## Runtime evidence

The release gate, the executor procedure for both notebooks and the durable execution record live in [`../docs/release-verification.md`](../docs/release-verification.md). CI separates static checks from execution. Release verification executes the notebooks through real IPython/Jupyter kernels, preserving `%pip` and notebook semantics; the companion receives the producer artifact and a distinct fresh inference CSV through explicit external paths. Hosted-Colab execution may still be recorded as an additional surface check, but static checks alone are never runtime evidence.

## AI provenance

These tutorials were developed with substantial AI assistance using **GPT-5.6 Sol High**, via **OpenAI / ChatGPT**, under Agent Relay role **Builder**, with maintainer direction and review. Attribution is provenance, not sign-off or independent verification.


## Sample dataset portfolio

The standalone E2E regressor tutorial includes two sample paths plus BYOD:

- **Diabetes** — numeric regression sanity check, built into scikit-learn (default; nothing is downloaded).
- **California Housing** — deterministic 2,000-row numeric subsample fetched through scikit-learn.
- **Insurance Medical Charges** — the mixed categorical/numeric archive under `examples/sample-data/` is **no longer fetched by the notebook**: a standalone notebook may not reach this repository (ST1), and the archive's raw GitHub URL was the only way to obtain it. Use it through `DATA_SOURCE = "Upload pre-split train/val/test"` by uploading its `train.csv` / `val.csv` / `test.csv`.

The external Insurance Medical Charges archive is downloaded from immutable repository revision `ec4d9e88846aac5dc478975c698703d38737575a` (retained by `anchors/sample-data-20260911`) and verified against its recorded SHA-256 (`7e16a06e13a7cb58fabb27ee53c1e20af83575c12ac97ea874fea45f013690a2`) before use. Dataset provenance, schemas, split construction, and licensing are documented in `../examples/sample-data/DATASET_CARD.md`.

## AI Assistance Disclosure

This repository’s code and accompanying documentation were developed with generative AI assistance for code development and technical writing under maintainer direction. The maintainer remains responsible for reviewing the implementation, validating results, and making release decisions. AI assistance does not constitute independent verification, provider endorsement, or release approval.
