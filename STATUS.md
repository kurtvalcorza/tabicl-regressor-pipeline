# Release status

Current status: **Candidate — standalone tutorial source complete, awaiting clean-runtime execution**. The public reference API (`src/tabicl_regressor_pipeline/api.py`, one module), the offline unit suite, `MODEL_CARD.md`, and the standalone tutorial pair — `tutorials/tabiclv2_regressor_colab.ipynb` (`E2E`) and `tutorials/tabiclv2_regressor_artifact_inference_colab.ipynb` (`ARTIFACT-INFERENCE`), both generated by `tools/build_notebook.py` under DIMER Notebook Specification 1.1 §3.6 — exist and pass static validation (`tools/validate_release_assets.py`, including the generator parity checks for both notebooks). The previous repository-installing pair (spec 1.0) was executed by the `release-notebook-execution` CI job through real kernels; that evidence does not transfer to the standalone carrier, and the CI job now needs the standalone executor procedure in `docs/release-verification.md`. The repository is not release-grade until clean-runtime execution evidence for the exact candidate commit of the standalone notebooks is recorded there.
